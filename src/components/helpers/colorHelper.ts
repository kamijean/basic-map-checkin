export const buttonColorMap = {
    primary: "#007bff",
    secondary: "#444",
    success: "#28a745",
    warning: "#ffc107",
    danger: "#dc3545",
  };
  
  export type ButtonColorLookup = keyof typeof buttonColorMap;
  